---
interface Props {
  level: number;  // 1–10
  segments?: number;
}

const { level, segments = 10 } = Astro.props;
---
<div class="gauge">
  {Array.from({ length: segments }).map((_, i) => (
    <div class={`gauge-segment ${i < level ? 'filled' : ''}`} />
  ))}
</div>
